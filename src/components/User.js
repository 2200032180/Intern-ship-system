import React from 'react';

const User = () => {
    return (
        <div style={{ padding: '20px' }}>
            <h1>Welcome to the User Dashboard!</h1>
            <p>This is where users can manage their internships and profiles.</p>
            {/* Add additional user functionalities here */}
        </div>
    );
};

export default User;
